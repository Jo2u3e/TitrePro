{% extends 'base.html.twig' %}

{% block title %}Nos Produits{% endblock %}


{% block content %}

   
<!--
   <div class="row">  
         
         {% for product in products %}
           <div class="col-md-4">
                  <div class="product-item text-center">
                    <img src="/uploads/{{product.illustration}}" alt="" width="250 px" height="250 px"   >
                    <h5 >  {{product.name}} </h5>
                    <span class="product-subtitle">  {{product.subtitle}} </span>
                    <span class="product-price"> {{product.price}} </span>
                  </div>
           </div>
         {% endfor %}
   </div>
-->

<div class="row">
     <div class="col-md-2">
         <h2> Filtrer </h2>
         {{form(form)}}
     </div>
     <div class="col-md-10">

     <h2> Nos Produits </h2>
     
          <div class="row">
      {% for product in products %}
        <div class="card product-item text-center" style="width: 18rem;">
          <a href=" {{path('product', {'slug' : product.slug}) }} ">  <img src="/uploads/{{product.illustration}}" class="card-img-top" alt="..." width="250 px" height="250 px" >  <a/>
           <div class="card-body">
              <h5 class="card-title"> {{product.name}} </h5>
              <span class="card-text product-subtitle ">{{product.subtitle}}</span>
              <span class="product-price"> {{ (product.price/100)|number_format(2) }}€ </span>
           </div>
       </div>

      {% endfor %}
   </div>
     </div>
</div>



   

   


{% endblock %}
